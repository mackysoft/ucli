interface:
  display_name: "uCLI Verify Changes"
  short_description: "Use when verifying uCLI-driven Unity changes with ucli verify profiles, claim packets, follow-up reads, ucli test, logs, artifacts, and opResults evidence after validation, plan, or call execution."
  default_prompt: "Use $ucli-verify-changes to follow the uCLI Verify Changes workflow."

policy:
  allow_implicit_invocation: true
