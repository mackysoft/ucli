interface:
  display_name: "uCLI Run Program"
  short_description: "Use when selecting, validating, planning, and safely supervising a closed typed uCLI Program across Unity lifecycle boundaries."
  default_prompt: "Use $ucli-run-program to follow the uCLI Run Program workflow."

policy:
  allow_implicit_invocation: true
