interface:
  display_name: "C# Authoring Rules"
  short_description: "C# のコードを実装、修正、レビューするときに、責務と契約の境界、既存実装への収束、依存関係、例外、非同期処理、リソースの寿命、言語機能、テスト容易性、性能の判断規則を適用する。"
  default_prompt: "Use $csharp-authoring-rules to follow the C# Authoring Rules workflow."

policy:
  allow_implicit_invocation: true
