---
name: commit
description: 変更を責務単位で分割し、Conventional Commits形式で、将来の保守者、レビュー担当者、障害調査者が変更意図を追跡できる件名と本文を持つコミットを作成する。
---

# 目的
- 1コミット=1責務を原則化し、レビューと障害調査の単位を明確にする。
- コミット本文に背景、変更内容、影響を残し、履歴から意図を追跡できるようにする。

# 使うタイミング
- コミットしたいとき

# 契約
- 1コミットは1つの責務と1つの変更理由に揃える。
- 未コミット差分が無い場合はコミットを作成しない。
- detached HEAD ではコミット作成を止め、作業ブランチへの切り替えを促す。

# 参照
- コミット規則: [references/commit-rules.md](references/commit-rules.md)
- コミット本文テンプレート: [assets/commit-body-template.md](assets/commit-body-template.md)

# 入力（任意。無くても推定して進める）
- 対象範囲（全差分 / 一部差分）
- 優先したい分割方針（厳密分割 / 実務優先）

# 出力
- 責務単位に分割されたコミット
- 参照ルールに従った件名と本文

# 手順
## 1. 前提確認
1. `git status --porcelain` を確認し、変更が無ければ終了する。
2. detached HEAD なら停止し、作業ブランチでの実行へ切り替える。

## 2. 変更を分類し、コミット単位を確定する
1. 変更ファイル一覧を取得する。
2. `references/commit-rules.md` に従って `type` / `scope` / コミット単位を確定する。

## 3. コミットメッセージを生成する
1. `references/commit-rules.md` に従って件名を作成する。
2. `references/commit-rules.md` の本文ルールに従い、将来の保守者、レビュー担当者、障害調査者が追跡できる説明へ整理する。
3. `assets/commit-body-template.md` を使って本文を作成する。

## 4. コミットを作成する
1. コミット単位ごとに対象ファイルのみ `git add` する。
2. `git commit -m "<subject>" -m "<body>"` でコミットする。
3. 作成後に `git log --oneline -n <作成数>` で結果を確認する。

# Definition of Done
- 変更がコミット種別単位で分割され、各コミットが単一種別を表している
- 各コミットが参照ルールに従った件名と本文を持つ
- 各コミット本文が変更理由、確認できる変更事実、影響を過大表現なく説明している
