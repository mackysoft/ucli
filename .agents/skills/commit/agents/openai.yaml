interface:
  display_name: "Commit"
  short_description: "責務単位で分割し、意図を追跡できるコミットを作成する"
  default_prompt: "Use $commit to split changes by responsibility and create conventional commits with traceable bodies."
policy:
  allow_implicit_invocation: true
