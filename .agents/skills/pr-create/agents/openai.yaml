interface:
  display_name: "PR Create"
  short_description: "テストからpush・PR作成までを一気通貫で実行する"
  default_prompt: "Use $pr-create to run required tests, call $push, and create or update a pull request in one flow."
policy:
  allow_implicit_invocation: true
