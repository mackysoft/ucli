interface:
  display_name: "PR Create"
  short_description: "検証からコミット・PR作成までを一気通貫で実行する"
  default_prompt: "Use $pr-create to run verification, commit, push, and create or update a pull request in one flow."
policy:
  allow_implicit_invocation: true
