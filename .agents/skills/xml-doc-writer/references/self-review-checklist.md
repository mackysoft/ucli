# 自己レビュー項目

- [ ] タグ本文は **English** で記述されている。
- [ ] `<summary>` は動作を誤解なく示している。
- [ ] 複数行の説明は `<para>` で分割されている。
- [ ] 契約がある `<param>` / `<returns>` / `<exception>` が記載されている（`cancellationToken` がキャンセル用途のみの場合を除く）。
- [ ] 入力制約（`null`、空、範囲、単位、前提条件）が必要な箇所で具体化されている。
- [ ] 戻り値保証（`null` 条件、空の意味、順序など）が必要な箇所で具体化されている。
- [ ] 例外条件が具体化され、実装と矛盾していない。
- [ ] `CancellationToken` を受け取るメンバで `OperationCanceledException` を `<exception>` に書いていない。
- [ ] タグ本文の前後にスペースがある。
- [ ] 命名の言い換えだけの文を追加していない。
