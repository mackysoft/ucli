interface:
  display_name: "Review Triage"
  short_description: "レビュー指摘の正当性を判定し、4分類トリアージと改善設計を行う"
  default_prompt: "Use $review-triage to assess pasted review comments, triage them into 採用/却下/保留/要確認, and produce an improvement design without implementing changes."
policy:
  allow_implicit_invocation: true
