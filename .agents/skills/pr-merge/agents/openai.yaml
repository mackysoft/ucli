interface:
  display_name: "PR Merge"
  short_description: "現在ブランチのPRをマージし、worktree対応で後始末する"
  default_prompt: "Use $pr-merge to merge the current branch pull request with state-based routing, then clean up remote and local branches using worktree-safe rules."
policy:
  allow_implicit_invocation: true
