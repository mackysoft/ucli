interface:
  display_name: "PR Merge"
  short_description: "CI状態を確認して現在ブランチのPRを安全にマージする"
  default_prompt: "Use $pr-merge to inspect PR CI/workflow state, merge the current pull request safely, and clean up branches."
policy:
  allow_implicit_invocation: true
