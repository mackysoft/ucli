interface:
  display_name: "Verification Gate"
  short_description: "変更が受け入れ可能かを判断するために、作業種別から必要な検証面を選び、リポジトリの専用 script や CI と同じコマンドを優先して実行し、成功、失敗、未検証範囲、次に必要な作業を残す。"
  default_prompt: "Use $verification-gate to follow the Verification Gate workflow."

policy:
  allow_implicit_invocation: true
